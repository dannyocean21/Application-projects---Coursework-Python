"""
SentimentClassifier - Теоретическое объяснение
==============================================

Классификатор SentimentClassifier — это нейронная сеть, разработанная для анализа тональности текста.
Он принимает текстовое предложение, закодированное в виде числовых индексов слов, и определяет, является
ли тональность предложения позитивной или негативной. Вот полное теоретическое объяснение его работы:

1. **Embedding слой**:
   - Embedding слой преобразует числовые индексы слов в плотные векторные представления фиксированной размерности.
   - Эти векторы содержат семантическую информацию о словах, например, слова с похожими значениями имеют схожие векторы.
   - Преобразование индексов в векторы позволяет модели лучше понимать контекст.

2. **Pooling**:
   - После преобразования каждого слова в вектор модель выполняет операцию усреднения (mean pooling).
   - Усреднение позволяет получить общий вектор предложения фиксированной длины, независимо от количества слов.
   - Таким образом, сеть обрабатывает входы переменной длины, агрегируя информацию о предложении.

3. **Полносвязный слой**:
   - Полносвязный слой (Linear) используется для преобразования вектора предложения в один выход.
   - Этот выход представляет собой скаляр, который интерпретируется как вероятность принадлежности к одному из классов.

4. **Активация (Sigmoid)**:
   - Для вывода используется сигмоидная функция активации, которая преобразует скалярное значение в диапазон [0, 1].
   - Значение ближе к 0 означает негативную тональность, ближе к 1 — позитивную.

5. **Обучение (Backpropagation)**:
   - Модель обучается с использованием функции потерь BCELoss (бинарная кросс-энтропия).
   - Она сравнивает предсказания модели с истинными метками и вычисляет ошибку.
   - Ошибка используется для обновления весов модели через алгоритм оптимизации (например, Adam).

Теперь перейдем к Python-коду с теоретическими пояснениями для каждой части.
"""