"""
BCELoss - Теоретическое объяснение
=================================

BCELoss (Binary Cross-Entropy Loss) — это функция потерь, используемая для задач бинарной классификации.
Она измеряет разницу между предсказанными вероятностями модели и истинными метками классов.

Основные аспекты BCELoss:

1. **Цель**:
   - В бинарной классификации мы предсказываем вероятность принадлежности объекта классу 1.
   - BCELoss помогает минимизировать ошибку между предсказанием модели (значения от 0 до 1) и истинными метками (0 или 1).

2. **Формула**:
   Функция потерь для одного примера вычисляется как:
   
       L = -[y * log(p) + (1 - y) * log(1 - p)]

   Где:
   - `y` — истинная метка (0 или 1).
   - `p` — предсказанная моделью вероятность класса 1 (выход сигмоидной функции).
   - `log` — натуральный логарифм.

   Если `y = 1`, используется первая часть формулы: `-log(p)`.
   Если `y = 0`, используется вторая часть: `-log(1 - p)`.

3. **Смысл**:
   - Когда модель делает верные предсказания (например, `p` близко к истинному `y`), значение потерь мало.
   - Если предсказание сильно отличается от истинной метки, потери велики.

4. **Применение**:
   - Обычно используется вместе с сигмоидной функцией активации на выходном слое модели.
   - Применяется для задач бинарной классификации или многоклассовой классификации с "one-vs-all".

Теперь рассмотрим, как BCELoss используется в PyTorch.
"""